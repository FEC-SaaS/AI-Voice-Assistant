import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contact Sales | CallTone AI",
  description: "Get in touch with the CallTone AI team. Discuss your needs and find the perfect AI voice agent plan.",
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children;
}
